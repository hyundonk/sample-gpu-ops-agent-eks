apiVersion: v2
name: demo-workload
description: Mock GPU worker Deployment and KEDA ScaledObject (redis list scaler) for the GPU agent demo
type: application
version: 0.2.0  # 0.2.0: drain-우선 종료 (grace 90s + DRAIN_BUDGET env)
